<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Movies" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Movies" parent="Base.Theme.Movies" />

    <style name="RoundedSquare">
        <item name="cornerSize">10%</item>
    </style>

    <style name="RoundedSquareDetails">
        <item name="cornerSize">4%</item>
    </style>

    <style name="ToolbarTextAppearance" parent="ThemeOverlay.AppCompat.Light">
        <item name="fontFamily">@font/poppins_semibold</item>
        <item name="android:textSize">26sp</item>
    </style>

</resources>